# Local overrides for Xcode build scripts.
# Copy to `.xcode.env.local` if you need to customize values.
# NODE_BINARY must remain portable so builds work on Intel + Apple Silicon hosts.
export NODE_BINARY=$(command -v node)
